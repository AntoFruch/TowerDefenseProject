public enum TileType
{
    EDGE, // GRAY
    PATH, // YELLOW
    SPAWN, // GREEN
    END, // RED
    INTERSECTION, // ORANGE

    CONSTRUCTIBLE, // WHITE

    VOID
}

public enum VertexLabel
    {
        START, END, INTERSECTION
    }